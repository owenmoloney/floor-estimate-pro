# Screenshots for the README

Add 2–3 PNGs here after capturing the running app, for example:

- `overview.png` — plan loaded with room / obstacle polygons
- `estimate.png` — toolbar + estimate result visible
- `calibrate.png` — (optional) calibration step

Then uncomment the image tags near the top of `README.md`.
